<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View License - Admin</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 min-h-screen flex flex-col">
    @include('components/navbar')

    <div class="flex-1 py-8">
        <div class="max-w-6xl mx-auto px-4">
            <!-- Header Section -->
            <div class="flex justify-between items-center mb-8">
                <div>
                    <h2 class="text-3xl font-bold text-gray-800">License Details</h2>
                    <p class="text-gray-600 mt-2">View complete license information</p>
                </div>
                <div class="flex space-x-4">
                    <a href="/admin/licenses/{{ license.id }}/edit" class="bg-blue-500 text-white px-6 py-2 rounded-md hover:bg-blue-600 transition-colors">
                        Edit License
                    </a>
                    <a href="/admin/licenses" class="bg-gray-500 text-white px-6 py-2 rounded-md hover:bg-gray-600 transition-colors">
                        Back to List
                    </a>
                </div>
            </div>

            <!-- License Information -->
            <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
                <!-- Main License Details -->
                <div class="lg:col-span-2 space-y-6">
                    <!-- License Key Card -->
                    <div class="bg-white rounded-lg shadow-md p-6">
                        <h3 class="text-xl font-semibold text-gray-800 mb-4">License Information</h3>
                        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                            <div>
                                <label class="block text-sm font-medium text-gray-500 mb-1">License Key</label>
                                <p class="text-lg font-mono text-gray-900">{{ license.licenseKey }}</p>
                            </div>
                            <div>
                                <label class="block text-sm font-medium text-gray-500 mb-1">Status</label>
                                <span class="px-3 py-1 inline-flex text-sm font-semibold rounded-full 
                                    {{ license.status === 'active' ? 'bg-green-100 text-green-800' : 
                                       license.status === 'expired' ? 'bg-red-100 text-red-800' : 
                                       'bg-yellow-100 text-yellow-800' }}">
                                    {{ license.status.charAt(0).toUpperCase() + license.status.slice(1) }}
                                </span>
                            </div>
                            <div>
                                <label class="block text-sm font-medium text-gray-500 mb-1">Start Date</label>
                                <p class="text-gray-900">{{ license.startDate.toFormat('dd/MM/yyyy') }}</p>
                            </div>
                            <div>
                                <label class="block text-sm font-medium text-gray-500 mb-1">End Date</label>
                                <p class="text-gray-900">{{ license.endDate.toFormat('dd/MM/yyyy') }}</p>
                            </div>
                        </div>
                    </div>

                    <!-- Company Information -->
                    <div class="bg-white rounded-lg shadow-md p-6">
                        <h3 class="text-xl font-semibold text-gray-800 mb-4">Company Information</h3>
                        <div class="space-y-4">
                            <div>
                                <label class="block text-sm font-medium text-gray-500 mb-1">Company Name</label>
                                <p class="text-gray-900">{{ license.companyName }}</p>
                            </div>
                            <div>
                                <label class="block text-sm font-medium text-gray-500 mb-1">Company Email</label>
                                <p class="text-gray-900">{{ license.companyEmail }}</p>
                            </div>
                            <div>
                                <label class="block text-sm font-medium text-gray-500 mb-1">Company Address</label>
                                <p class="text-gray-900">{{ license.companyAddress || 'Not specified' }}</p>
                            </div>
                        </div>
                    </div>

                    <!-- Point of Contact -->
                    <div class="bg-white rounded-lg shadow-md p-6">
                        <h3 class="text-xl font-semibold text-gray-800 mb-4">Point of Contact</h3>
                        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                            <div>
                                <label class="block text-sm font-medium text-gray-500 mb-1">PIC Name</label>
                                <p class="text-gray-900">{{ license.picName }}</p>
                            </div>
                            <div>
                                <label class="block text-sm font-medium text-gray-500 mb-1">PIC Phone</label>
                                <p class="text-gray-900">{{ license.picPhone }}</p>
                            </div>
                        </div>
                    </div>

                    <!-- Support Response Times -->
                    @if(license.supportResponseTimes && license.supportResponseTimes.length > 0)
                        <div class="bg-white rounded-lg shadow-md p-6">
                            <h3 class="text-xl font-semibold text-gray-800 mb-4">Support Response Times</h3>
                            <div class="space-y-4">
                                @each(support in license.supportResponseTimes)
                                    <div class="border border-gray-200 rounded-lg p-4">
                                        <div class="grid grid-cols-1 md:grid-cols-2 gap-4 mb-3">
                                            <div>
                                                <label class="block text-sm font-medium text-gray-500 mb-1">Category</label>
                                                <p class="text-gray-900 font-semibold">{{ support.category }}</p>
                                            </div>
                                            <div>
                                                <label class="block text-sm font-medium text-gray-500 mb-1">Response Time</label>
                                                <p class="text-gray-900">{{ support.responseTime }}</p>
                                            </div>
                                        </div>
                                        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                                            <div>
                                                <label class="block text-sm font-medium text-gray-500 mb-1">Description</label>
                                                <p class="text-gray-900">{{ support.description }}</p>
                                            </div>
                                            <div>
                                                <label class="block text-sm font-medium text-gray-500 mb-1">Resolution</label>
                                                <p class="text-gray-900">{{ support.resolution }}</p>
                                            </div>
                                        </div>
                                    </div>
                                @end
                            </div>
                        </div>
                    @endif
                </div>

                <!-- Sidebar -->
                <div class="space-y-6">
                    <!-- Quick Actions -->
                    <div class="bg-white rounded-lg shadow-md p-6">
                        <h3 class="text-lg font-semibold text-gray-800 mb-4">Quick Actions</h3>
                        <div class="space-y-3">
                            <a href="/admin/licenses/{{ license.id }}/edit" class="w-full bg-blue-500 text-white px-4 py-2 rounded-md hover:bg-blue-600 transition-colors text-center block">
                                Edit License
                            </a>
                            <form method="POST" action="/admin/licenses/{{ license.id }}" class="w-full" onsubmit="return confirm('Are you sure you want to delete this license?')">
                                <input type="hidden" name="_method" value="DELETE">
                                <button type="submit" class="w-full bg-red-500 text-white px-4 py-2 rounded-md hover:bg-red-600 transition-colors">
                                    Delete License
                                </button>
                            </form>
                        </div>
                    </div>

                    <!-- License Status -->
                    <div class="bg-white rounded-lg shadow-md p-6">
                        <h3 class="text-lg font-semibold text-gray-800 mb-4">License Status</h3>
                        <div class="text-center">
                            <div class="text-3xl font-bold mb-2 
                                {{ license.status === 'active' ? 'text-green-600' : 
                                   license.status === 'expired' ? 'text-red-600' : 
                                   'text-yellow-600' }}">
                                {{ license.status.charAt(0).toUpperCase() + license.status.slice(1) }}
                            </div>
                            <p class="text-gray-600">
                                @if(license.status === 'active')
                                    License is currently active and valid
                                @elseif(license.status === 'expired')
                                    License has expired
                                @else
                                    License is currently suspended
                                @endif
                            </p>
                        </div>
                    </div>

                    <!-- License Validity -->
                    <div class="bg-white rounded-lg shadow-md p-6">
                        <h3 class="text-lg font-semibold text-gray-800 mb-4">Validity Period</h3>
                        <div class="space-y-3">
                            <div class="flex justify-between">
                                <span class="text-gray-600">Start Date:</span>
                                <span class="font-medium">{{ license.startDate.toFormat('dd/MM/yyyy') }}</span>
                            </div>
                            <div class="flex justify-between">
                                <span class="text-gray-600">End Date:</span>
                                <span class="font-medium">{{ license.endDate.toFormat('dd/MM/yyyy') }}</span>
                            </div>
                            <div class="border-t pt-3">
                                <div class="flex justify-between">
                                    <span class="text-gray-600">Duration:</span>
                                    <span class="font-medium">
                                        @php
                                            const startDate = new Date(license.startDate);
                                            const endDate = new Date(license.endDate);
                                            const diffTime = Math.abs(endDate - startDate);
                                            const diffDays = Math.ceil(diffTime / (1000 * 60 * 60 * 24));
                                            echo diffDays + ' days';
                                        @endphp
                                    </span>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    @include('components/footer')
</body>
</html>
